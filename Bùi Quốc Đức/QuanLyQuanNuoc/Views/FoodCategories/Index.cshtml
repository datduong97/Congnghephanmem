@model IEnumerable<QuanLyQuanNuoc.Models.FoodCategory>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CategoryFoodName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CategoryFoodName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdCategoryFood }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdCategoryFood }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdCategoryFood })
        </td>
    </tr>
}

</table>
